class Solution {
    public List<Boolean> prefixesDivBy5(int[] nums) {
        List<Boolean> answer = new ArrayList<>();
        int rem = 0;

        for (int n:nums){
            rem = (rem * 2 + n) % 5;
            answer.add(rem == 0);
        }
        return answer;
    }
}
