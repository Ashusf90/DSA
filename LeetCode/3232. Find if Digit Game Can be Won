class Solution {
    public boolean canAliceWin(int[] nums) {
        int n=0;
        int m=0;
        for(int num:nums){
            if(num<10){
                n+=num;
            }else{
                m+=num;
            }
        }
        return n!=m;
    }
}
